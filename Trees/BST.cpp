#include<bits/stdc++.h>
using namespace std;

class BST {
private:
	int data;
	BST* left;
	BST* right;
public:
	BST(int val) {
		data = val;
		left = right = NULL:
	}

}

void insert(int data){
	 root;
	if(temp == NULL){ }
	while(temp != NULL) {
		if(data <= temp->data){
			temp = temp->left;
		} else if(data > temp->data) {
			temp = temp->right;
		}
	}
	
}

int mian(){

	return 0;
}
